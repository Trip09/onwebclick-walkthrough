{% extends '::Frontend/layout.html.twig' %}

{% block content -%}
    <h1>MovieRented creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_movie_rented') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
